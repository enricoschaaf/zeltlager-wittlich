import { UpdateForm } from "components/UpdateForm"
import { formatTitle } from "utils/formatTitle"

const Update = () => <UpdateForm />

export const metadata = {
  title: formatTitle("Anpassen"),
}

export default Update
