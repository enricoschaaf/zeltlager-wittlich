import { RegistrationForm } from "components/RegistrationForm"
import { formatTitle } from "utils/formatTitle"

const Register = () => <RegistrationForm />

export const metadata = {
  title: formatTitle("Anmelden"),
}

export default Register
